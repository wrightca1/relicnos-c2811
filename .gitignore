# The generated header is a build product -- regenerate it from firmware/.
cd2481_ucode.h

# IOS images are not distributed here: 122 MB, past GitHub's file limit, and
# unnecessary since the extracted microcode is in firmware/.
*.BIN

# build products
vmlinuz*
initramfs.cpio.gz
initramfs_root/
*.o
*.elf
__pycache__/
